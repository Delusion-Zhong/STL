#include <iostream>
#include <algorithm>
#include <vector>
#include "../include/main.h"
using namespace std;

int Find_nums(vector<int> nums, int target)
{
    int num = nums.size() - 1;
    int i = num / 2;
    if (i<target)
    {
        
    }
    
}
int main()
{
    cin.get();
    return 0;
}